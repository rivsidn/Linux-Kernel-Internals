



| 函数                | 作用                       | 中断处理                      | 调用场景                         |
|---------------------|----------------------------|-------------------------------|----------------------------------|
| spin_lock_irqsave() | 禁用中断并保存当前中断状态 | 保存原始中断标志到 flags 变量 | 可能在中断已禁用的上下文中调用   |
| spin_lock_irq()     | 直接禁用中断               | 假设中断原本是开启的          | 确定在进程上下文中，中断是开启的 |






