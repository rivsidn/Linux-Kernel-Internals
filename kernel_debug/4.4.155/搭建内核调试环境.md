
`Linux 4.4.155` 内核调试环境基于`Ubuntu 16.04` .



## 内核编译

## 创建根文件系统

### busybox编译

- 源码获取

  ```bash
  # 开启系统deb-src 源
  sudo apt-get update
  sudo apt-get source busybox
  ```
- 编译选项设置

  ```bash
  # 配置开启CONFIG_STATIC=y
  make menuconfig
  # 编译
  make && make install
  ```

### 根文件系统

```bash
# BusyBox目录下
cd _install
mkdir proc
mkdir sys
touch init
chmod +x init
find . | cpio -o --format=newc > ../rootfs.img
```

```bash
# init内容如下
#!/bin/sh
echo "{==DBG==} INIT SCRIPT"
mkdir /tmp
mount -t proc none /proc
mount -t sysfs none /sys
mount -t debugfs none /sys/kernel/debug
mount -t tmpfs none /tmp

mdev -s 
echo -e "{==DBG==} Boot took $(cut -d' ' -f1 /proc/uptime) seconds"

# normal user
setsid /bin/cttyhack setuidgid 1000 /bin/sh
```

如果要设置root 用户，需要将1000 设置为 0.




