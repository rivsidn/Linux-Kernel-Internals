

- 为什么要用这种形式 `-%ERESTARTSYS` 表示错误码.

- 类似这种形式的错误码

  ```c
    if (IS_ERR(kthread[i])) {
        ret = PTR_ERR(kthread[i]);  // 应该保存实际错误码
        pr_err("failed create kernel thread\n");
        goto err;
    }
  
  ```





