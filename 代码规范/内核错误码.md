
虚拟地址的最后一个page 的地址永远不会被指向，所以这里可以被使用作ERROR 号.


| 函数      | 说明             |
|-----------|------------------|
| ERR_PTR() | 出错码转换成指针 |
| PTR_ERR() | 指针转换成出错码 |
| IS_ERR()  | 判断指针是否出错 |


ERROR 就是一个标识，有时候可以被用作正数、有时候被用作负数(-ENOMEM)，看函数间的约定.

返回值为指针的时候，用作负数(-ENOMEM) 标识一个错误的指针地址.


